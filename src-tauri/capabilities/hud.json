{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hud",
  "description": "The HUD can read its telemetry and compact state, receive events, and move or resize its own window. System changes, account writes, cleanup and updater commands belong to the main window.",
  "windows": ["hud"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-size",
    "allow-hud-snapshot",
    "allow-hud-is-compact"
  ]
}
